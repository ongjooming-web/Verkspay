# 2026-03-18 — Prism Phase 3: Billing Debug & Auth Fixes

## Session Summary
Debugging and fixing upgrade button issues in Prism subscription billing system. Root cause: improper JWT decoding in client-side billing section.

## Work Completed

### 1. Settings Page Cleanup (commit `0a8b8ba`)
- Removed old hardcoded "Billing & Plan" card
- Added "Upgrade →" button to Current Plan display
- Button positioned flex with justify-between

### 2. Error Logging Enhancement (commit `0139158`)
- Added detailed error messages to upgrade button
- Console logging at each step of upgrade flow
- Alert users with specific error instead of generic message

### 3. Billing Endpoint Auth Fix (commit `17c1e1f`)
- Replaced manual JWT decode with token validation
- Added token audience verification (aud === 'authenticated')
- Applied to both:
  - `/api/billing/create-checkout-session`
  - `/api/billing/customer-portal`

### 4. Security Audit & Fixes (commit `1372553`)
- **Issue #1**: ENV VAR NAMES
  - Changed `NEXT_PUBLIC_STRIPE_PRICE_PRO` → `STRIPE_PRICE_PRO`
  - Server-side routes must NOT use NEXT_PUBLIC_ prefix
  - Action required: Update Vercel env vars (delete old, add new)
  
- **Issue #2**: JWT Decode Safety
  - Added token audience validation
  - Verified token structure (3 parts)
  
- **Issue #3**: Duplicate Subscription Prevention
  - Check if user already has active subscription before charging
  - Return 400 with clear message if duplicate attempt
  
- **Issue #4**: Webhook Handler
  - Verified webhook already handles subscription lifecycle

### 5. TypeScript Fix (commit `d804f28`)
- Added `stripe_customer_id` to profile select query in checkout endpoint

### 6. Client Auth Fix - UUID Error Resolution (commit `8052a64`)
- **Root cause**: Using JWT decode in Settings page instead of proper Supabase auth
- **Fix**: Updated BillingSection component
  - `loadBillingData()`: Use `supabase.auth.getUser()` instead of JWT decode
  - `handleUpgrade()`: Refresh session, then use `supabase.auth.getSession()`
  - `handleManageSubscription()`: Same pattern as upgrade
- **Result**: No more UUID validation errors

## UUID Error Details
- **Error**: "@supabase/auth-js: Expected parameter to be UUID but is not"
- **Location**: Settings page → Upgrade button click
- **Root cause**: Manual JWT decoding was unsafe and producing invalid UUID
- **Solution**: Use proper Supabase auth methods (`getUser()`, `getSession()`)
- **Status**: Fixed in client code; if still failing, check backend response

## Current Code Status

### Settings Page (`prism/src/app/settings/page.tsx`)
- **BillingSection component** is complete and correct
- Uses `supabase.auth.getUser()` for initial load
- Refresh session in both `handleUpgrade()` and `handleManageSubscription()`
- Proper error handling and user-facing alerts

### Billing Endpoints
- `create-checkout-session`: Validates token, checks duplicate subscription, creates Stripe customer
- `customer-portal`: Opens Stripe portal for subscription management
- Both use token audience verification

## Pending Actions
1. ✅ Update Vercel env vars (delete NEXT_PUBLIC_STRIPE_PRICE_*, add STRIPE_PRICE_*)
2. Wait for Vercel redeploy
3. Test upgrade button end-to-end
4. If UUID error persists: Check browser console for exact error location

## Key Learnings
- Never use `NEXT_PUBLIC_` prefix in server-side routes
- Always use `supabase.auth.getUser()` / `getSession()` instead of manual JWT decode
- Refresh session before getting token to ensure validity
- Duplicate subscription prevention is critical for payment security
- Add comprehensive logging for debugging auth flow issues

## Commits This Session
- `0a8b8ba` - Clean up old billing section + add upgrade button
- `0139158` - Add detailed error messages to upgrade button
- `17c1e1f` - Fix billing auth - decode JWT directly with verification
- `1372553` - Security audit: Fix 4 critical issues in billing endpoints
- `d804f28` - Fix: Add stripe_customer_id to select query
- `8052a64` - Fix: Use proper Supabase auth instead of JWT decode in Settings

## Testing Checklist
- [ ] Vercel redeploy complete
- [ ] Settings page loads without errors
- [ ] Click "Upgrade to Pro" → should redirect to Stripe
- [ ] Complete Stripe test payment → webhook fires → tier updates
- [ ] Free tier user can no longer create 6th invoice (should see upgrade modal)

---

## Session 2 (2026-03-18 20:51-22:31 GMT+8) - Partial Payments & Invoice Status Fix

### Major Issue Discovered & Fixed
**Problem**: Invoice creation button not working on invoices page
- **Root cause**: Database constraint mismatch
- **Error**: "new row for relation 'invoices' violates check constraint 'invoices_status_check'"
- **Why**: Code was setting `status: 'unpaid'` but DB constraint only allowed old values (draft, sent, paid, overdue)

### Database Migration Executed
Ran comprehensive migration (`add_partial_payments.sql`) that:
- ✅ Fixed status constraint to allow: `unpaid`, `paid`, `paid_partial`, `overdue`
- ✅ Added `amount_paid` (NUMERIC DEFAULT 0) column to invoices
- ✅ Added `remaining_balance` (NUMERIC) column to invoices
- ✅ Created `payment_records` table (id, invoice_id, amount, payment_method, payment_date, notes, created_at, updated_at)
- ✅ Created index on `payment_records.invoice_id`
- ✅ Enabled RLS on payment_records table
- ✅ Added RLS policies (users can view/insert only their own payment records)
- ✅ Populated `remaining_balance = amount - amount_paid` for all existing invoices

### Partial Payments Feature - Backend Complete
Built complete partial payment infrastructure:

**1. `POST /api/invoices/[id]/record-payment` endpoint**
- Records manual partial payments
- Input: amount, paymentMethod, paymentDate, notes
- Creates entry in `payment_records` table
- Updates invoice `amount_paid`, `remaining_balance`
- Auto-sets status based on totals:
  - amount_paid >= amount → status = 'paid'
  - amount_paid > 0 and < amount → status = 'paid_partial'
  - amount_paid = 0 → status = 'unpaid'
- Returns updated invoice and payment record

**2. `POST /api/invoices/[id]/partial-payment-link` endpoint**
- Creates Stripe Payment Link for partial amount
- Input: amount, clientEmail
- Validates: amount > 0, amount <= remaining_balance
- Creates Stripe product + price dynamically
- Creates payment link with metadata (invoiceId, freelancerId, partialAmount)
- Returns payment_url for client
- Webhook will auto-update totals when client pays

**3. `PartialPaymentModal.tsx` component**
- Reusable modal with two modes: manual | stripe
- **Manual mode**: amount, paymentMethod dropdown (stripe/bank/cash/check/crypto), paymentDate, notes
- **Stripe mode**: amount (pre-filled with remaining_balance), clientEmail
- Validation: amount > 0, amount <= remaining_balance
- Error handling with user-facing alerts
- Calls appropriate endpoint on submit

### Code Cleanup Done
- Added form validation to invoice creation (checks all fields filled)
- Added error messages so users see what failed
- All new invoices default to `status: 'unpaid'` ✅

### Next Session Work (Documented in HANDOFF.md)
🚧 **Partial Payment UI Integration** - Need to add to `/app/invoices/[id]/page.tsx`:
- Two buttons: "💰 Record Manual Payment" and "💳 Create Partial Payment Link"
- Display payment summary card (total / paid / remaining)
- Fetch and display payment history list
- Wire up PartialPaymentModal component
- Test end-to-end flows

Full code template provided in HANDOFF.md (copy-paste ready)

### Commits This Session
- `727cf91` - Add form validation and error handling to invoice creation
- `f63c306` - Add partial payments migration SQL
- `716fe16` - Add Partial Payment feature - API endpoints and modal component
- `ce3cece` - Add HANDOFF.md for session continuity

### HANDOFF.md Created
Comprehensive handoff document with:
- Current state (everything completed)
- Incomplete work (exact code to add for UI)
- Known bugs (3 issues to fix: Stripe onboarding, "From:" field blank, old status comments)
- Next session instructions (clear prompt)
- Key file locations (all important paths)
- Environment variables (full list)
- Success metrics (testing checklist)
- Long-term roadmap (future phases)
