# Daily Log: Wednesday 2026-03-18

## Prism Phase 3 Progress

### Major Wins Today (GMT+8)

**Stripe Integration & USD Migration (9:00-9:24 AM)**
- ✅ Migrated entire codebase from USDC (crypto) to USD (fiat-only)
- ✅ Stripe Connect Express onboarding working (callback fires, account ID persists)
- ✅ Stripe callback handler: simplified to mark complete on return (pragmatic approach)
- ✅ Payment modal refactored to support two payment methods:
  - **Stripe Bank**: Shows recipient, account ID, amount in USD (no gas fees)
  - **Wallet (fallback)**: Shows wallet address (legacy flow)
- ✅ Component renamed: `USDCPaymentCard` → `PaymentCard` (reflects dual-method support)
- ✅ Profile now tracks: `payment_method` field ('bank' or 'crypto')
- ✅ Auth flow fixed: Session refresh before mark-paid (handles token expiration)

### Commits Today
1. `d519889` - Change payment language USDC→USD, format Stripe recipient
2. `74ba94f` - Update StripeConnect text to USD
3. `eabd4c8` - Add detailed logging to Stripe callback flow
4. `fbd2fd7` - Replace USDC refs with USD in payment modal
5. `a0d1d7c` - Add Stripe bank payment flow to payment modal
6. `6570b09` - Fix missing closing CardBody tag
7. `79d46a0` - Refine PaymentCard to use payment_method field
8. `b98edac` - Fix auth token validation in mark-paid flow

### Current Issues (In Progress)
- **Mark as Paid auth error**: "Unauthorized: Invalid token" 
  - Fixed: Now refreshes session before sending token
  - Will verify on next deployment + user test

### Architecture Decisions Made
- **Stripe-first, Wallet-fallback**: Primary payment method is now Stripe bank transfer
- **No WalletConnect**: Removed complex routing, kept MetaMask/Phantom only
- **Plain integer chain IDs**: `[1, 8453]` not CAIP-2 strings
- **Pragmatic test mode handling**: Mark as complete on callback (not relying on API flags)
- **State param over cookies**: Base64-encoded userId in Stripe callback state
- **Upsert pattern**: Profile update fails → upsert to create missing row

### Next Steps
1. Test mark-paid flow after Vercel redeploy (verify token refresh works)
2. Verify Stripe payment recipient displays correctly on invoices
3. Test end-to-end: create invoice → mark paid → see Stripe recipient
4. Mobile testing (MetaMask/Phantom deep linking)
5. Add "Generate Payment Link" button to invoice detail
6. Test Stripe webhook marking invoice paid

### Key Files Modified
- `PaymentCard.tsx` - Dual payment method support
- `StripeConnect.tsx` - USD language updates
- `/api/stripe/connect/route.ts` - Enhanced logging
- `/api/stripe/connect/resume/route.ts` - Enhanced logging
- `/api/stripe/connect/return/route.ts` - Pragmatic completion logic
- `/api/invoices/[id]/mark-paid/route.ts` - Better error logging

### Technical Details
- **Stripe API version**: 2024-06-20 (all routes)
- **Payment recipient format**: "Stripe Bank • acct_1TC7..."
- **Database**: `profiles.payment_method` determines which flow to use
- **Mobile support**: Deep linking for MetaMask/Phantom (not WalletConnect)
- **No prism billing yet**: Free MVP to validate market fit first

**Later Commits (10:00-10:28 AM):**
9. `235d3ca` - Fix import PaymentCard from correct filename
10. `04b742a` - Fix remaining USDC references, improve token auth flow
11. `7256727` - Fix TypeScript const/let issue in token handling
12. `dbbcf65` - Remove legacy USDC payment modal
13. `5a239e9` - Fix auth token validation - decode JWT directly
14. `595c3c9` - Remove dead code calling admin.getUserById with empty string
15. `cc9c2e4` - Build Payment Links feature complete (Phase 1+2)
16. `89b3e04` - Fix Stripe Payment Link API - use price instead of price_data

### Payment Links Feature (NEW)
- ✅ Public `/pay/[invoiceId]` page (no auth required)
- ✅ `GET /api/invoices/[id]/public` endpoint (public invoice fetch)
- ✅ `POST /api/stripe/payment-link` (create Stripe Payment Link)
- ✅ Webhook auto-marks invoice paid on `payment_intent.succeeded`
- ✅ "Share Payment Link" button copies `app.prismops.xyz/pay/[id]`
- ✅ Client email required before payment
- ✅ Freelancer Stripe account receives payment directly


