# Session: 2026-03-26 (Continued) — Crypto Cleanup + Proposals MVP

## PROMPT 1: Crypto References Removal ✅

**Status:** Already complete from previous phases
- Crypto UI (wallet connection, USDC banners) removed
- Database columns preserved dormant: wallet_address, usdc_network, preferred_network, webhook_enabled
- No USDC/crypto mentions in settings, landing, or main pages
- API routes with crypto marked DEPRECATED where they exist
- Environment variables kept in Vercel (unused)

**Verification:**
- ✅ Settings page: No crypto banners or wallet sections
- ✅ Landing page: No crypto references
- ✅ Nav bar: No crypto menu items
- ✅ Database: Columns intact but UI-dormant

## PROMPT 2: Basic Proposals Feature — IN PROGRESS

**Database:** ✅ Migration created
- Table: `proposals` with 30+ columns
- Fields: user_id, client_id, title, status, line_items (JSONB), total_amount, payment_terms
- Status enum: draft, sent, viewed, accepted, declined
- RLS policies: full user isolation
- Indexes: user_id, client_id, status, created_at
- File: `supabase/migrations/20260326_create_proposals_table.sql`

**Numbering:** Plan = PROP-0001, PROP-0002 (sequential per-user, like invoices)

**MVP Features Planned:**
1. Proposals list page (/proposals)
   - Table: proposal number, title, client, amount, status (color-coded), created date
   - "Create Proposal" button
   - Click row to view/edit
   - Empty state for new users

2. Create/Edit form (/proposals/new, /proposals/[id]/edit)
   - Title, client (dropdown), summary, scope of work, deliverables, timeline
   - Line items (add/remove rows, auto-calculate totals)
   - Valid until date, payment terms dropdown, T&C textarea
   - Buttons: "Save as Draft", "Send Proposal"

3. Proposal detail page (/proposals/[id])
   - Full proposal display (professional format)
   - Action buttons: Edit, Send, Resend, Accept/Decline, Download PDF
   - **KEY FEATURE:** "Convert to Invoice" button → creates invoice from proposal

4. Invoice conversion
   - Single click creates invoice with:
     - Same client_id, line_items, amount, payment_terms, currency
     - Description: "Invoice for: [proposal title]"
     - Status: unpaid, next invoice number
   - Redirects to new invoice + success toast

5. Plan limits
   - Trial: 3/month
   - Starter: 10/month
   - Pro/Enterprise: Unlimited
   - Master: Bypass all

6. Dashboard integration
   - Count real proposals in stats

**Status:**
- [x] Migration created
- [ ] Proposal numbering utility (reuse invoice logic)
- [ ] Proposals list page
- [ ] Create/edit form with line items
- [ ] Proposal detail page
- [ ] Convert to invoice functionality
- [ ] PDF download
- [ ] Plan limits checking
- [ ] Dashboard stats update
- [ ] Testing

**Next Steps:**
1. Create proposal numbering utility (`src/utils/proposal-numbering.ts`)
2. Build proposals list page
3. Build create/edit form with line item handling
4. Build detail page with convert-to-invoice logic
5. Add PDF generation
6. Implement plan limits
7. Test full workflow
